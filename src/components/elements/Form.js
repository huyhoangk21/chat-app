import styled from 'styled-components';

const Form = styled.form`
  background-color: ${props => props.theme.white};
  width: 90%;
  max-width: 400px;
  height: 400px;
  padding: 2rem 1rem;
`;

export default Form;
